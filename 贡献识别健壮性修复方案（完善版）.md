# AI/人工贡献识别健壮性修复方案（完善版）

- 文档日期：2026-09-19
- 适用范围：`操作系统实验数据清洗/AI人工贡献识别工具/`（v3）
- 文档状态：实施方案，尚未实施
- 关联原始方案：[贡献识别健壮性修复计划.md](贡献识别健壮性修复计划.md)

## 1. 结论与范围

已确认的问题是模型偶尔不能遵守受控读取后的 JSON 提交信封：返回内容无法解析为 JSON、根节点不是对象、`action` 缺失或错误，或缺失应提交的对象。这类错误在相同输入上具有偶发性；现有实现虽提供一次局部修复，但局部修复仍失败后会直接固化为不可重试的失败记录。

本次采用最小闭环修复：保留现有局部修复，在其耗尽后以全新上下文完整重跑一次任务。单任务总尝试次数固定为 **2**（首次执行加一次完整重跑）。本次不修改上游清洗、贡献判定规则、报告结构或诚信判别系统。

## 2. 已证实事实

| 事实 | 当前实现位置 | 影响 |
| --- | --- | --- |
| `_controlled_exchange` 对协议错误只进行一次修复 | `contribution_recognition/analysis.py` | 第二次信封错误直接抛出 |
| `_submission_from_response` 将错误/缺失的提交动作视为 `AssessmentValidationError` | `analysis.py` | 无法区分偶发的协议失配与内容校验失败 |
| `_execute_task` 只调用一次 `ContributionAnalyzer.analyze` | `contribution_recognition/cli.py` | 无法使用全新会话摆脱异常上下文 |
| `_persist_failure` 只把可重试 `NimError` 标记为 `retryable` | `cli.py` | 受控协议偶发失败被写为不可重试 |
| 普通 `batch` 对已有失败保守跳过 | `cli.py` | 需要 `--retry-failed` 或 `--force` 才再次调用模型 |

本例中读取轮数未打满，已有连续成功复现，故不把读取预算、材料缺失或 API 配置当作主因。

## 3. 目标与不变量

### 目标

1. 单次偶发的提交信封错误在无人干预下可完成一次全新会话重跑。
2. 只有明确的信封错误进入任务级重试；内容错误、资料问题和配置问题不扩大调用次数。
3. 两次都失败时，保留结构化失败和脱敏诊断，绝不写入未经独立复核的归因。
4. 已失败任务跨运行仍由操作者显式决定是否重试。

### 必须保持

- `insufficient_data` 仍不调用语义模型，且不被重试为归因结论。
- 模型密钥、原始响应、思维内容和完整上下文不得写入 assessment、报告、清单或运行日志。
- assessment 与双报告继续通过原子写入一并提交；失败不产生半写的成功产物。
- 现有传输层 429、5xx、连接中断和流截断重试保持不变。

## 4. 详细设计

### 4.1 错误分类

在 `analysis.py` 新增 `TransientProtocolError`，继承 `AssessmentValidationError`。它只表达“可从全新模型输出恢复的提交信封错误”，以确保现有捕获 `AssessmentValidationError` 的局部修复逻辑仍可复用。

下列位置必须改为抛出 `TransientProtocolError`：

| 条件 | 来源 | 任务级行为 |
| --- | --- | --- |
| 可见内容无法解析为 JSON 对象 | `_parse_json_object` | 局部修复后仍失败时完整重跑 |
| JSON 根节点不是对象 | `_parse_json_object` | 同上 |
| `action` 缺失或不是本阶段要求的提交动作 | `_submission_from_response` | 同上 |
| 提交字段缺失或不是对象 | `_submission_from_response` | 同上 |

以下情况仍抛出普通 `AssessmentValidationError` 或既有错误类型，不进入任务级重试：受控读取请求的越界、引用未读材料、证据或 schema 内容校验失败、主分析未读取任何证据、独立复核内容不合法、资料不足、输入损坏、路径不安全和 NIM 配置错误。`NimError` 继续仅按其既有 `retryable` 属性处理。

### 4.2 两层恢复流程

```text
单个阶段模型回复
  -> 现有一次 protocol/submission repair
  -> 成功：继续当前任务
  -> TransientProtocolError：任务级判断剩余次数
       -> 第 1 次任务：丢弃整个会话，重新创建 client/analyzer，重跑主分析 + 独立复核
       -> 第 2 次任务：写入结构化 failed
  -> 其他错误：立即写入结构化 failed
```

实现要求：

1. 在 `cli.py:_execute_task` 中，NIM 配置读取、快照构建和缓存判断完成后，包裹 `ContributionAnalyzer(...).analyze(...)` 的调用。
2. `MAX_TASK_ATTEMPTS = 2` 是模块常量；不新增 CLI 参数，避免把本次小范围修复扩展为运行策略配置面。
3. 每次尝试重新实例化 `NimStreamingClient` 和 `ContributionAnalyzer`，不得复用失败尝试中的 `messages`、responses、excerpts 或局部修复消息。
4. 仅当捕获到 `TransientProtocolError` 且还未达到上限时继续。无需任务级 sleep；传输层已有独立退避，模型信封抖动通过新会话而非延时处理。
5. 主分析或独立复核中任一阶段出现此错误，都重跑整个任务；不得把未完成独立复核的主分析结果持久化。

### 4.3 失败状态、日志与批处理

`_persist_failure` 应把 `TransientProtocolError` 视为可重试，与 `NimError(retryable=True)` 保持一致。失败 assessment 的 `errors[0].retryable` 因此为 `true`；不修改 assessment 或 manifest schema。

每一次未完成的任务尝试写入一条脱敏运行事件，最终失败仍保留 `analysis_failed`。新增或补充的字段只允许为：

```json
{
  "event": "analysis_attempt_failed",
  "stage": "semantic_analysis_or_review",
  "task_attempt": 1,
  "max_task_attempts": 2,
  "error_code": "TransientProtocolError",
  "retryable": true
}
```

禁止记录模型返回正文、提示词、messages、思维内容、密钥、材料原文或可逆的敏感片段。错误消息继续经现有脱敏函数处理。

批处理策略不变：普通 `batch` 发现已有 `failed` 记录时继续返回 `skipped_failed`；只有 `batch --retry-failed` 或 `--force` 才重新执行。这样，单次偶发错误已由本次两次尝试处理，而连续失败不会在每次常规批处理时持续消耗额度。

## 5. 实施清单

| 文件 | 必需改动 |
| --- | --- |
| `contribution_recognition/analysis.py` | 增加 `TransientProtocolError`；仅把四类提交信封错误切换到该类型；保留现有一次局部修复和所有内容校验语义。 |
| `contribution_recognition/cli.py` | 增加两次任务级尝试常量和完整重跑循环；重新实例化 client/analyzer；记录脱敏尝试事件；持久化时正确计算 `retryable`。 |
| `test_semantic_protocol.py` 及 CLI 测试文件 | 按测试对接文档补充脚本化假客户端测试。 |
| `README.md` | 补充“提交信封偶发失败最多完整重跑一次”和跨运行需 `--retry-failed` 的说明。 |

不修改 `storage.py`、manifest schema、`nim_client.py`、NIM 采样参数、读取轮数或备用模型配置。

## 6. 验收标准与回滚

满足以下条件即可验收：

1. 首次或局部修复后的信封错误可触发一次全新完整任务，成功时只写入最终完整 assessment 和两份报告。
2. 连续两次信封错误后，任务以 `failed` 结束，`errors[0].retryable` 为 `true`，总任务尝试次数恰为 2。
3. 内容校验错误、资料不足和配置错误只执行一次任务级尝试。
4. 普通 `batch` 仍跳过既有失败；`--retry-failed` 与 `--force` 可重新处理。
5. 新增日志和产物不包含密钥、原始响应或思维内容。

回滚只需撤销本次 `analysis.py`、`cli.py`、README 与测试改动；既有失败 assessment 仍可由 `--retry-failed` 重新处理，无需迁移或删除用户产物。

## 7. 明确不做的事项

以下内容不属于本次变更，除非后续有运行数据证明必要性：备用模型、批次熔断、跨运行自动重试、清单字段或版本迁移、读取预算调整、关闭 thinking、温度调整、传输重试抖动，以及对既有无关回归测试的修复。
