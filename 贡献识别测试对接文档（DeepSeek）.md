# AI/人工贡献识别健壮性修复测试对接文档（DeepSeek）

- 文档日期：2026-09-19
- 测试对象：`操作系统实验数据清洗/AI人工贡献识别工具/`
- 对应方案：[贡献识别健壮性修复方案（完善版）.md](贡献识别健壮性修复方案（完善版）.md)
- 测试边界：验证实现；不调整需求、不扩大修复范围、不调用真实 NIM。

## 1. 测试目标

验证实现仅为偶发的 JSON 提交信封错误增加一次全新任务重跑，并且不会把资料不足、配置错误或内容校验错误误判为可重试。测试应优先使用脚本化假客户端和临时目录，避免消耗 NIM 额度、读取真实学生材料或暴露任何密钥。

目标行为：单任务包含首次执行和一次完整重跑，最多两次。每次完整重跑均覆盖主分析与独立复核；成功前不持久化未复核的归因；跨运行重新尝试失败记录仍须明确传入 `--retry-failed` 或 `--force`。

## 2. 执行约束

1. 在工具目录执行测试：`操作系统实验数据清洗/AI人工贡献识别工具/`。
2. 使用项目已有 Python 解释器，例如 `py -m unittest discover -v`；如环境没有 `py`，使用对应的 Python 命令，但在报告中说明。
3. 所有新增测试使用 fake/scripted client、mock 或临时目录；不得设置真实 `NVIDIA_API_KEY`，不得发起真实网络请求。
4. 不得在测试输出、断言失败信息、补丁、日志摘录或结果报告中写入 API 密钥、原始模型响应、`reasoning_content`、完整 prompts 或完整材料正文。
5. 未经需求方确认，不修复与本需求无关的既有失败；应单列为基线问题。

## 3. 必测项目与结果要求

| ID | 场景与建议位置 | 关键操作 | 通过要求 |
| --- | --- | --- | --- |
| T01 | 信封错误分类，`test_semantic_protocol.py` | 分别构造不可解析 JSON、数组根节点、错误 action、缺失提交对象。 | 都抛出 `TransientProtocolError`；引用越界和语义字段错误仍为普通 `AssessmentValidationError`。 |
| T02 | 现有局部修复回归 | 第一次提交 action 错误，repair 后返回有效提交。 | 仅发生一次任务级尝试；局部 repair 生效；最终 assessment 完整且独立复核完成。 |
| T03 | 新增任务级重跑 | 第一次完整任务在局部 repair 后仍返回错误 action；第二次完整任务返回有效主分析和复核。 | `analyze` 被完整调用 2 次；第二次使用新 client/analyzer 或可证明的新 messages；只持久化成功 assessment 和两份报告。 |
| T04 | 重试耗尽 | 两次完整任务都在提交信封阶段失败。 | 返回 `failed`；任务级调用数恰为 2；`errors[0].retryable == true`；运行日志含脱敏 `task_attempt`、上限、阶段和错误类型。 |
| T05 | 确定性错误不重试 | 分别注入证据越界/内容校验错误、资料不足、NIM 配置错误。 | 每个场景只进行 1 次任务级尝试；资料不足不调用模型；最终错误的 `retryable` 不因本次改动变为 true。 |
| T06 | 传输层回归，`test_nim_client.py` | 运行既有 503、截断和非重试错误测试。 | 既有传输重试次数与退避语义不改变；传输错误不会被新的协议分类吞没。 |
| T07 | 批处理保守策略 | 为同一任务预置失败 assessment，分别执行普通 batch、`batch --retry-failed` 和 `batch --force`。 | 普通 batch 返回 `skipped_failed` 且不调用模型；后两者允许重新执行。 |
| T08 | 缓存与原子写入 | 成功后重复相同输入运行；另在两次失败场景检查输出目录。 | 成功任务命中现有缓存；失败不留下伪造的 `complete` assessment 或半成对报告/清单。 |
| T09 | 脱敏回归 | 在异常消息和脚本化响应中放入 `nvapi-` 样式字符串及 `reasoning_content`。 | assessment、Markdown 报告、manifest 与运行日志均不含敏感值、原始响应或思维字段。 |
| T10 | 全量回归 | 执行完整 unittest 发现。 | 新增测试全部通过；既有失败按第 4 节分类报告。 |

T03 和 T04 是本次修复的核心验收项。测试应断言完整任务重跑，而不是只断言某个单独 `complete()` 调用被再次执行。

## 4. 已知基线问题的处理

当前仓库存在至少一项与本需求无关的可疑断言：`test_v3_reporting.py` 对同一字符串同时使用 `assertIn("[REDACTED]")` 与 `assertNotIn("[REDACTED]")`。这类问题应按以下方式处理：

1. 先记录完整测试命令、失败测试名和最小失败摘要。
2. 标记为“基线问题，未修改”，除非本次实现直接改变了该行为。
3. 不把基线问题计入 T01--T09 的失败，不顺手修改断言或扩大修复提交。
4. 在最终报告中区分“本次修复失败”和“基线失败/环境问题”。

## 5. 结果提交要求

请提交一份 Markdown 测试报告，按以下模板填写；不要粘贴原始模型文本或敏感配置。

```markdown
# 贡献识别健壮性修复测试报告

- 测试日期：
- 提交/版本标识：
- Python 版本：
- 执行目录：
- 实际执行命令：
- 是否使用真实 NIM：否

## 汇总

| 分类 | 通过 | 失败 | 阻塞 | 备注 |
| --- | ---: | ---: | ---: | --- |
| 新增测试（T01--T09） |  |  |  |  |
| 既有回归 |  |  |  |  |

## 逐项结果

| ID | 结果 | 关键断言/证据 | 运行日志或产物路径（脱敏） | 备注 |
| --- | --- | --- | --- | --- |
| T01 | PASS/FAIL/BLOCKED |  |  |  |

## 失败与基线问题

| 类型 | 测试名或场景 | 最小错误摘要 | 是否与本次修复相关 | 处理建议 |
| --- | --- | --- | --- | --- |

## 结论

- 是否满足本次验收：是/否/有阻塞
- 未满足项：
- 未执行项及原因：
```

测试通过的最低条件是 T01--T09 全部 PASS，T10 中除已明确记录的无关基线问题外没有新增回归；并能证明任务级尝试上限为 2、确定性错误不重试、失败时不存在未经复核的贡献结论。
