# 贡献识别健壮性修复测试报告

- 测试日期：2026-09-19
- 提交/版本标识：当前工作树（未提交）。修复实现为工作区改动：`contribution_recognition/analysis.py`、`contribution_recognition/cli.py`；本次测试新增 `test_task_retry.py` 与 `test_semantic_protocol.py` 中的分类用例
- Python 版本：3.12.7（Anaconda，`D:\\Anaconda3\\python.exe`）
- 执行目录：`操作系统实验数据清洗/AI人工贡献识别工具/`
- 实际执行命令：
  - `python -B -m unittest test_task_retry -v`
  - `python -B -m unittest test_semantic_protocol -v`
  - `python -B -m unittest test_nim_client -v`
  - `python -B -m unittest discover`
- 是否使用真实 NIM：否（脚本化假客户端 + 临时目录；未设置 `NVIDIA_API_KEY`，未发起网络请求；不读取真实学生材料）

> 说明：环境未安装 `py` 启动器（`Get-Command py` 为空），故使用项目同一解释器的 `python` 命令。

## 被测行为（实现快照）

- `analysis.py` 新增 `TransientProtocolError(AssessmentValidationError)`；仅四类“提交信封”错误改抛该类型：内容无法解析为 JSON 对象、根节点非对象、`action` 缺失/错误、提交字段缺失或非对象。
- `cli.py` 新增 `MAX_TASK_ATTEMPTS = 2`：`_execute_task` 在局部修复耗尽后以**全新** `NimStreamingClient`/`ContributionAnalyzer` 完整重跑一次；两次均失败才写入结构化 `failed`。
- `_persist_failure` 将 `TransientProtocolError` 记为 `retryable: true`，并在运行日志写入脱敏 `analysis_attempt_failed`（含 `task_attempt`、`max_task_attempts`、`stage`、`error_code`）。
- 资料不足、配置错误、内容校验错误只执行一次任务级尝试；普通 `batch` 仍对既有失败返回 `skipped_failed`。

## 汇总

| 分类 | 通过 | 失败 | 阻塞 | 备注 |
| --- | ---: | ---: | ---: | --- |
| 新增测试（T01--T09） | 21 | 0 | 0 | `test_task_retry` 13 项 + T01 分类 8 项 |
| 既有回归 | 24 | 3 | 0 | 3 项为基线失败，未修改，与本次修复无关 |

全量发现：`Ran 48 tests`，`FAILED (failures=3)`（= 27 既有 + 21 新增）。

## 逐项结果

| ID | 结果 | 关键断言/证据 | 运行日志或产物路径（脱敏） | 备注 |
| --- | --- | --- | --- | --- |
| T01 | PASS | 8 项：不可解析 JSON、数组根、错误 action、缺失提交对象、提交对象非对象均抛 `TransientProtocolError`；引用越界与弱证据语义错误仍为普通 `AssessmentValidationError`；`ControlledReadError` 非瞬态 | `test_semantic_protocol.py`：`TransientProtocolClassificationTests` | 位于文档建议的 `test_semantic_protocol.py` |
| T02 | PASS | 首次提交 action 错误 → 局部 `protocol_repair` 生效；仅 1 个任务级 client；client 恰 5 次调用；最终 `analysis_status=complete`、`review.status=agreed` | `test_task_retry.py`：`test_local_repair_needs_only_one_task_attempt` | 断言到第 3 次请求含 `protocol_repair` |
| T03 | PASS | 第 1 次任务局部修复后仍失败 → 第 2 次任务用**另一个** client 实例（`IsNot`），第 2 次会话不含第 1 次的 `protocol_repair`；只持久化成功 assessment 与两份报告 | `test_envelope_failure_after_local_repair_triggers_fresh_rerun` | 核心验收项 |
| T04 | PASS | 两次任务均在信封阶段失败：结果 `failed`；client 恰 2 个；`errors[0].code=TransientProtocolError` 且 `retryable=true`；日志含 1 条 `analysis_attempt_failed`（attempt=1,max=2,stage,error）与 1 条 `analysis_failed`（attempt=2） | `test_two_envelope_failures_end_as_retryable_failed` | 核心验收项 |
| T05 | PASS | (a) 证据越界：1 个 client、3 次调用后 `failed`、`retryable=false`；(b) 资料不足：0 个 client、`insufficient_data`、`errors=[]`；(c) 配置错误：0 个 client、`failed`、`retryable=false` | `DeterministicErrorsAreNotRetriedTests`（3 项） | 确定性错误不扩大调用 |
| T06 | PASS | 既有 `test_nim_client` 4 项全过：503/截断/非重试错误的次数与退避语义不变 | `python -B -m unittest test_nim_client` → `Ran 4 tests OK` | 未新增传输层测试 |
| T07 | PASS | 预置失败后：普通 `batch` → `skipped_failed` 且 0 个 client；`--retry-failed` 与 `--force` 各重新执行并 `complete`（各 1 个 client） | `BatchConservativePolicyTests`（3 项） | 经 `cli._batch`，捕获其 JSON 输出 |
| T08 | PASS | 成功后再跑同输入命中缓存（`cached`/`rerendered`）且 0 个 client；失败后 assessment 仍为 `failed`、`cache_hit=false`、失败 assessment 与两份报告哈希一致（原子成对） | `CacheAndAtomicityTests`（2 项） | 无伪造 `complete`、无半写成对产物 |
| T09 | PASS | 响应与异常消息注入 `nvapi-` 样式串与 `reasoning_content`：学生目录全部产物不含 token/思维字段，含 `[REDACTED]`；错误消息已脱敏 | `RedactionRegressionTests`（2 项） | 读取 assessment/报告/manifest/运行日志全文扫描 |
| T10 | PASS（含基线） | `Ran 48 tests`：21 新增全过；3 项既有失败见下节 | `python -B -m unittest discover` | 无本次修复引入的新增回归 |

## 失败与基线问题

| 类型 | 测试名或场景 | 最小错误摘要 | 是否与本次修复相关 | 处理建议 |
| --- | --- | --- | --- | --- |
| 基线失败 | `test_repository.ContributionRepositoryV2Tests.test_lab1_missing_a_required_process_material_is_not_analysis_ready` | `'代码差异报告\\lab1.md' != '代码差异报告/lab1.md'`（Windows 路径分隔符） | 否 | 单独修复断言（改用 `os.sep`/纯 POSIX 断言），不计入本次 |
| 基线失败 | `test_v3_reporting.V3ReportingTests.test_renderers_use_short_redacted_evidence_only` | `'[REDACTED]' unexpectedly found`；该用例对同一字符串同时 `assertIn` 与 `assertNotIn`（第 133--134 行），自相矛盾 | 否 | 需澄清预期后单独修复断言 |
| 基线失败 | `test_v3_reporting.V3ReportingTests.test_storage_commits_assessment_and_both_reports_with_v3_manifest` | `'[REDACTED]' unexpectedly found`；fixture 摘录本身含 `NVIDIA_API_KEY=[REDACTED]` | 否 | 同上 |

以上三项在实施修复前即存在（相同的三条失败），本次实现未改动其代码路径；按对接文档第 4 节标记为“基线问题，未修改”。

## 结论

- 是否满足本次验收：**是**
- 依据：
  1. T03 证明单次信封错误可在同一次运行内以**全新会话**完整重跑一次并成功，且只持久化成功结果；
  2. T04 证明连续两次信封错误后任务级尝试上限恰为 2、`retryable=true`、诊断字段齐全；
  3. T05 证明资料不足/配置错误/内容校验错误均只尝试 1 次，资料不足不调用模型；
  4. T07 证明普通 `batch` 仍保守跳过既有失败，仅 `--retry-failed`/`--force` 重新执行；
  5. T08/T09 证明缓存与原子写入不变、无伪造成功产物、无密钥/思维字段泄漏。
- 未满足项：无（T01--T09 全部 PASS）。
- 未执行项及原因：无。测试未设置真实 `NVIDIA_API_KEY`，未调用真实 NIM（符合对接文档第 2 节约束）。

---

## 附录 A：真实 NIM 端到端验证（2026-09-19，额外执行）

本附录记录在真实 NIM 凭据下执行的端到端验证，不属于 T01--T09 的假客户端测试。执行目录同上。

### A.1 凭据与连接

```
python -B identify_contributions.py doctor
→ {"attempts":1,"model":"nvidia/nemotron-3-super-120b-a12b","protocol":"host-mediated-read-material-json","status":"ok"}  EXIT=0
```

### A.2 真实批处理重试（`batch --retry-failed`）

```
python -B identify_contributions.py batch --retry-failed
→ task_count=46，EXIT=0；failed=0，skipped_failed=0
```

- 此前为 `failed/skipped_failed` 的两项真实任务（历史原因含未配置凭据）在真实 NIM 下完成：
  - `戴炜 lab1`：`analysis_completed`，`task_attempt=1`，`max_task_attempts=2`，`primary_read_count=4`，`primary_repaired=true`。
  - `李兆卓 lab1`：`analysis_completed`，`task_attempt=1`，`max_task_attempts=2`，`primary_read_count=4`。
- 总览 `AI人工贡献识别汇总/总览.md` 共 46 行，`failed` 行数 = 0。

### A.3 原始失败用例的真实重跑（关键证据）

对最初失败的 2406080118 杨涵清 lab1 执行 `analyze --student 杨涵清 --lab lab1 --force`（同一输入指纹 `sha256:b5760ba9…`），真实复现并验证了修复：

| 运行日志（`AI人工贡献识别运行日志/杨涵清/lab1/runs/`） | 事件 | 关键字段 |
| --- | --- | --- |
| `20260919T050754.137679Z.jsonl` | `analysis_attempt_failed` | `task_attempt=1`，`max_task_attempts=2`，`error_code=TransientProtocolError` |
| `20260919T050938.540686Z.jsonl` | `analysis_completed` | `task_attempt=2`，`max_task_attempts=2`，`primary_read_count=2` |

即：第 1 次任务因提交信封错误失败 → 修复以全新会话完整重跑 → 第 2 次成功。最终 `assessment_lab1.json` 为 `analysis_status=complete`（7 个贡献单元，`review.status=disagreed`），EXIT=0。

### A.4 安全复核

- 对 `操作系统实验数据记录-已清洗` 全目录扫描 `nvapi-`：**无命中**（密钥未进入 assessment、报告、清单或运行日志）。
- 真实运行产物仅新增 assessment/双报告/脱敏运行日志，未改动原始提交与基准目录。

### A.5 真实测试结论

- 真实 NIM 路径跑通：`doctor` ok、`batch --retry-failed` 46 任务 0 失败。
- **原始失败指纹在真实模型下再次触发 `TransientProtocolError`，并由修复的任务级重跑自动恢复为 complete**，与 T03 的假客户端断言一致。

