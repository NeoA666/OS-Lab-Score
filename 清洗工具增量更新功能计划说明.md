# 实验过程清洗工具增量处理计划

## 摘要

为 `实验过程清洗工具` 增加默认启用的学生级增量处理。每次运行仍扫描全部输入，但只对原始录像、计时文件或时间线日志发生变化的学生重新生成对应产物。

范围仅包含 `replay_term_qa.py` 产生的四类终端报告和实验过程时间线；独立的“简洁实验过程时间线”生成器保持不变。删除的原始提交不删除既有输出，而是在批处理总览中标注“原始提交已不存在，输出已保留”。

## 实现变更

- 在现有归属清单中扩展增量字段，不创建新的跨学生缓存系统：
  - 学生目录下 `.replay_term_qa.json` 记录终端报告阶段状态、输入指纹、处理器签名和报告汇总摘要。
  - `实验过程时间线/.timeline_manifest.json` 记录时间线阶段的同类信息。
  - 保留所有既有归属、来源、产物清单与安全清理字段，确保差异报告工具和旧输出兼容。

- 新增可复用的输入快照函数，按排序后的相对路径和文件原始字节 SHA-256 计算确定性指纹：
  - 终端报告阶段：`term/*.out.gz`，以及每个录像同名 `.tim.gz` 的存在状态和内容。
  - 时间线阶段：终端报告阶段输入，加 `logs/events.jsonl` 的存在状态和内容。
  - 忽略 `labs/`、`transcripts/`、其他日志、文档和生成目录，因为它们不参与这两个处理阶段。
  - 仅扫描约定文件，使用流式哈希，不解压、不重放；当前数据规模下全量指纹计算开销极低。

- 为两个阶段分别计算启动时的处理器签名：
  - 终端报告：`replay_term_qa.py` 内容摘要和缓存格式版本。
  - 时间线：`replay_term_qa.py`、`timeline_alignment.py`、`timeline_reports.py` 内容摘要和缓存格式版本。
  - 清洗或渲染代码变化后自动失效对应缓存，无需手工清理。

- 默认运行流程按每名学生、每个启用阶段独立判断：
  - 首次运行、旧清单、指纹变化、处理器变化、来源路径变化、状态不是完整成功、产物缺失/符号链接/越界、清单损坏时，完整重建该学生该阶段。
  - 指纹与签名一致、来源一致、归属清单有效且全部登记产物为普通文件时，跳过该阶段并输出“增量跳过（输入及产物未变）”。
  - 报告和时间线可分别命中。例如仅修改 `logs/events.jsonl` 时，只重建时间线。
  - 报告“部分失败”或时间线存在处理失败时不写可命中的成功状态，下次自动重试。
  - 正常完成后才原子写入新的增量字段，避免异常中断留下错误缓存状态。

- 新增 `--force` 参数：
  - 忽略当前所选学生、所选阶段的增量命中条件，强制完整重建。
  - 与现有 `--student`、`--timeline-only`、`--no-timeline` 组合使用。
  - `--timeline-only` 只检查时间线缓存；`--no-timeline` 只检查终端报告缓存；默认模式分别判断两个阶段。

- 缓存命中时从清单恢复最小汇总数据，继续参与根目录 `README.md` 的统计，确保“全部跳过”不会把统计错误写成零。
  - 总览增加每位学生的阶段状态：已处理、增量跳过、部分失败或失败。
  - 默认全量批处理额外扫描已清洗目录中未被当前输入分配的工具归属目录；若其原始来源已不存在，则保留产物并在总览列为“原始提交已不存在，输出已保留”。
  - 筛选运行继续只汇总当前筛选范围，不把未筛选学生重建、删除或重新归属。
  - 根 README 保持现有“本次运行”语义，使用本次处理、缓存摘要和缺失来源信息生成准确总览。

## 测试计划

- 首次运行写入两个阶段的增量信息；相同输入二次运行不调用终端重放或时间线构建，学生产物保持不变。
- 修改、新增、删除 `.out.gz` 或匹配 `.tim.gz` 时，仅重建受影响学生的报告和时间线。
- 仅修改 `logs/events.jsonl` 时，仅重建该学生时间线，四类终端报告字节不变。
- 修改 `labs/`、`transcripts/` 或无关文件时不触发重建。
- 缺失计时文件、补回计时文件、缺失已登记产物、损坏清单、旧版清单、来源目录改名、处理器代码签名变化和 `--force` 均触发正确重建。
- 验证报告阶段失败/部分失败与时间线阶段失败不会被缓存为成功，下一次运行会重试。
- 验证 `--timeline-only`、`--no-timeline`、`--student` 与 `--force` 的阶段隔离和统计行为。
- 验证已删除原始提交的输出不会被删除，并在默认全量运行的 README 中明确标记。
- 保留并运行现有终端报告、时间线归属、安全清理和幂等性测试；新增调用计数与产物哈希断言验证真正跳过昂贵重放。

## 默认约定

- 增量以“学生提交目录 + 处理阶段”为最小粒度，不实现逐录像缓存。
- 输入内容哈希优先于文件修改时间或大小，避免拷贝、同步和归档整理导致误判。
- 原始提交目录改名会重建一次，以刷新报告中展示的来源路径；之后可再次命中缓存。
- 不检测人工直接修改但仍存在的工具生成 Markdown 内容；工具清单声明的产物视为工具管理范围，缺失时会自动恢复。
- 不纳入 xv6 代码差异报告工具，也不改造独立的简洁时间线生成器。
