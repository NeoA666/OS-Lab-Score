# 操作系统学生实验过程数据背景总结

> 整理日期：2026-09-15  
> 适用范围：本项目中“操作系统实验”任务及其原始数据、清洗结果、处理工具和验证记录。  
> 说明：本文总结数据背景与处理规则，不逐一复述学生提交的 xv6 源码、实验报告或每条终端内容。

## 1. 项目定位

本项目用于采集、保存、重放和整理操作系统学生实验过程数据，重点回答以下问题：

- 学生在终端中执行了哪些 Shell 命令；
- 命令发生在哪个实验目录、哪个工作目录；
- 学生是否在实验过程中使用 Claude Code，以及形成了哪些有效问答；
- 终端画面如何随时间变化；
- 如何把原始录像转换为可审阅、可统计、可回查的实验过程记录；
- 在不修改原始学生数据的前提下，如何验证处理结果没有遗漏、重复或跨学生混淆。

项目根目录下的任务分工如下：

| 目录或文件 | 作用 |
| --- | --- |
| `操作系统实验/操作系统实验数据记录/` | 学生原始实验数据 |
| `操作系统实验/操作系统实验数据记录-已清洗/` | 按学生整理的派生报告和时间线 |
| `操作系统实验/replay_term_qa.py` | 当前终端录像重放、报告生成入口 |
| `操作系统实验/timeline_alignment.py` | 事件与终端观察时间、原始定位信息对齐 |
| `操作系统实验/timeline_reports.py` | 时间线 JSON/Markdown 生成与安全写入 |
| `操作系统实验/历史验证记录/` | 历史脚本、日志、缓存和验收材料，仅作归档 |
| `公共依赖/pylib/` | 终端重放使用的 `pyte`、`wcwidth` 等依赖 |

## 2. 当前数据规模

截至 2026-09-15，工作区中可见的操作系统实验过程数据规模为：

| 指标 | 数量或状态 |
| --- | ---: |
| 原始学生目录 | 12 |
| `.out.gz` 终端输出录像 | 134 |
| `.tim.gz` timing 计时文件 | 134 |
| 清洗结果中的学生目录 | 12 |
| 时间线 JSON 文件 | 19 |
| 时间线事件总数 | 332 |
| Shell 命令事件 | 262 |
| Claude 用户问题事件 | 35 |
| Claude 回复事件 | 35 |

历史验收记录显示，正式时间线验收覆盖 12 名学生、134 个录像；321 个事件具有带时区的绝对显示时间，11 个事件只能保留录像相对时间，没有完全缺少观察时间的事件。这里的数量是当前项目处理链路的记录规模，不是对学生实际全部操作的统计保证。

## 3. 原始数据背景

### 3.1 学生目录命名

原始学生目录通常采用：

```text
学号-姓名-YYYYMMDD-HHMM/
```

也支持带实验提交标识和重复编号的变体。处理工具从目录名提取学号、姓名和采集时间：

- 学号从目录左侧匹配；
- 日期、时间和可选重复编号从目录右侧匹配；
- 中间部分整体作为姓名，因此姓名可包含连字符；
- 非法日期或时间不会被擅自修正，而是保留原值并标注无法解析；
- 目录名不规范时仍可处理录像，但学生元数据可能记为“未知”。

### 3.2 终端录像

每个学生目录可包含：

```text
学生目录/
├── logs/
│   └── events.jsonl
├── term/
│   ├── 录像编号.out.gz
│   └── 录像编号.tim.gz
└── transcripts/
```

其中：

- `.out.gz` 是 Linux `script` 产生的压缩 pty 输出流，包含终端文本和 ANSI 控制序列；
- `.tim.gz` 是与输出流配对的计时文件，每行记录“延迟 秒数 + 输出字节数”；
- 两个文件通过相同的录像编号配对；
- `logs/events.jsonl` 可提供 Claude 会话启动记录、时间戳、终端和进程信息；
- `transcripts/` 属于原始数据的一部分，但当前 Claude QA 提取不直接依赖它，而是从终端画面重放结果中识别问答。

原始文件被视为证据源。当前处理工具只读取原始数据，不移动、删除或改写原始录像。

## 4. 数据处理链路

整体链路可以概括为：

```text
学生原始目录
    -> 读取 script 头、pty 输出和 timing
    -> pyte 终端重放
    -> Shell 命令提取
    -> Claude TUI 逐帧 Screen/diff 提取
    -> 根据工作目录识别 lab
    -> 结合 session 日志对齐观察时间
    -> 生成学生级 Markdown 报告和 JSON/Markdown 时间线
    -> 测试、哈希和内容一致性验收
```

处理入口位于 `操作系统实验/replay_term_qa.py`。默认同时生成原有四类报告和实验过程时间线，也可使用：

```powershell
python replay_term_qa.py "操作系统实验数据记录" `
  --output "操作系统实验数据记录-已清洗"
```

仅更新时间线时使用 `--timeline-only`；只生成原有报告时使用 `--no-timeline`。历史验证脚本不作为当前运行入口。

## 5. 实验分类规则

实验分类支持 `lab0` 至 `lab8`，分类依据是 Shell 提示符中显示的工作目录，而不是正文中提到的实验编号。

规则包括：

- `~/lab0`、`/home/user/lab1/kernel` 等完整路径段可识别为对应实验；
- `lab10`、`lab0-copy` 等不会误识别为 `lab0`；
- 命令参数、学生文本或 Claude 回复中提到其他 lab，不改变分类；
- `cd` 命令归入执行它时所在的目录；
- 只有后续 Shell 提示符显示新目录后，后续命令才切换到新 lab；
- 失败的 `cd` 不改变当前分类；
- 一个录像跨越多个 lab 时按终端提示符分段；
- 无法归入 `lab0`–`lab8` 的内容归入 `other`；
- Claude 问答沿用 Claude 启动时所在的 Shell 工作目录，不根据 Claude 文本中的路径重新分类。

学生总计中的录像数和 Claude 会话数按原录像去重，因此不能简单把各 lab 文件中的录像数或会话数相加。命令数和问答轮次可以按事件相加。

## 6. 派生报告及其含义

每名学生的清洗结果通常位于：

```text
学生/
├── 终端对话记录/
├── 完整终端转写记录/
├── 终端命令统计/
├── claude对话/
└── 实验过程时间线/
```

### 6.1 终端对话记录

`terminal_qa_report_labN.md` 和 `terminal_qa_report_other.md`：

- 按命令组织 Shell 过程；
- 保留完整命令、工作目录、录像编号、开始时间和相对时间；
- 展示关联命令输出；
- 记录命令频次；
- 单条输出遵循原报告的行数上限，长输出可能被截断。

### 6.2 完整终端转写记录

`full_terminal_transcript_labN.md` 和 `full_terminal_transcript_other.md`：

- 通过 `pyte` 重放终端控制序列；
- 保留连续文本、滚屏和清屏历史；
- 保留可见的 Claude TUI、Shell 和普通输出；
- 不使用 Claude QA 的过滤规则；
- 记录录像重放错误；
- 跨 lab 录像按片段输出，并保留片段和原始字节范围。

它更接近“终端发生了什么”的完整可读复原，但不等同于原始字节文件本身。

### 6.3 终端命令统计

`command_statistics_labN.md` 和 `command_statistics_other.md` 只统计 Shell 命令，按：

1. 执行次数降序；
2. 命令文本升序；

进行排列。命令会保留参数、选项、管道和重定向。Shell 中用于启动 Claude 的命令也会作为 Shell 命令统计；Claude TUI 中以 `❯` 开头的用户输入不会重复计入 Shell 命令。

### 6.4 Claude 对话

`claude_qa_clean_labN.md` 和 `claude_qa_clean_other.md`：

- 从 `pyte` 逐帧 Screen diff 中识别 Claude TUI；
- 以 `❯` 识别用户问题，以 `●` 识别 Claude 回复；
- 合并流式增长和重复重绘；
- 过滤 logo、边框、spinner、thinking、工具状态、权限菜单、登录错误、状态栏、快捷键和 VS Code 浮层等界面噪声；
- 只保留完整有效的用户问题与最终回复；
- 每个录像至多对应一个 Claude Session，同一录像跨 lab 返回时按规则合并并连续编号 Turn；
- 只有启动界面、未完成回复或缺少完整问答的录像不计入有效问答。

因此，“没有提取到有效 Claude 对话”不等同于“学生没有使用 Claude”；它只表示当前识别规则没有保留一组完整问答。

## 7. 实验过程时间线

时间线是独立于原有四类报告的结构化事件视图。每名学生按实际出现的 lab 生成：

```text
实验过程时间线/
├── timeline_lab0.json
├── timeline_lab0.md
├── timeline_lab1.json
├── timeline_lab1.md
└── .timeline_manifest.json
```

时间线事件类型为：

- `shell_command_observed`：观察到的 Shell 命令；
- `claude_user_observed`：观察到的 Claude 用户问题；
- `claude_reply_observed`：观察到的 Claude 回复。

JSON 事件通常保留：

- 学号、姓名和 lab；
- 工作目录、录像编号、终端信息；
- `observed_at`：带时区的绝对显示时间；
- `elapsed_seconds`：相对于录像起点的累计 timing 偏移；
- `source`：`.out.gz`、`.tim.gz`、timing 行号、画面编号和解压字节范围；
- 关联事件编号；
- 回复首次完整显示时间；
- 不确定性和异常说明。

时间线中的“时间”必须按观察语义理解：

- Shell 命令时间是命令回显末端所在输出块的观察时间，不是提交时间或执行开始时间；
- Claude 用户问题时间是问题文本首次完整显示时间，不是按下回车的时间；
- Claude 回复时间是回复正文开始显示或最终完整可见的观察时间，不是模型开始生成或生成完成时间；
- 录像起始时钟通常只有秒级精度，显示毫秒只是保留 timing 相对偏移；
- 相同或接近的显示时间不证明严格先后或因果关系；
- 若绝对时钟冲突，则保留录像相对时间并增加不确定性说明，不强行伪造全局顺序。

## 8. 异常、不确定性和边界

当前处理链路明确保留以下风险：

- 缺少或损坏 timing 文件时，可退化为一次性重放，但时间线和问答定位可能不完整；
- `script` 未记录运行期间的终端 resize 时，只能根据 TUI 边框推断列数；
- 多次 resize 或 Claude TUI 版本变化可能影响终端重放和界面过滤；
- 被原地重绘覆盖、没有进入滚屏历史的瞬时字符不会作为独立文本导出；
- 日志时间倒序、录像头时间与 session 日志不一致时，会记录警告而不自动改写原始时钟；
- 录像起始时间只有秒级精度，目录名采集时间本身不包含时区；
- `other` 既可能表示非实验目录，也可能表示目录未知、提示符无法识别或失败录像；
- 报告中的“异常”不一定等于处理失败，可能只是时间或来源证据不足；
- 没有事件或没有有效问答，不代表学生没有进行相应操作。

## 9. 数据隔离与安全写入

处理工具按学生隔离输入和输出，避免同名学生、重复采集或不同来源结果被合并：

- 学生输出目录使用姓名、学号、采集标识等信息消解重名；
- `.replay_term_qa.json` 和 `.timeline_manifest.json` 记录派生结果的来源归属；
- 默认只允许替换由同一来源生成的报告；
- 不覆盖被标记为其他来源的结果；
- 输出文件采用临时文件加原子替换；
- 时间线目录和归属清单拒绝符号链接；
- 旧报告只在确认属于同一来源且新报告全部成功写入后清理；
- 原始录像、日志和其他学生文件不被修改。

## 10. 当前验收结论

历史验收记录（2026-09-12）表明：

- 12 名学生、134 个录像均纳入时间线验收；
- 332 个事件中，321 个有绝对显示时间，11 个仅有相对时间；
- 262 条 Shell 命令按原录像逐条比对，正文和工作目录无差异；
- 70 个旧问答文本块按学生、lab、录像和角色比对，均保留；
- 事件编号唯一，关联关系未跨学生、未跨录像；
- 原始 11,576 个文件及 72 个旧输出完成路径和 SHA-256 一致性校验；
- 46 项测试中 45 项通过，1 项 Windows 符号链接测试因权限限制跳过；
- 正式时间线输出生成 19 组 Markdown/JSON 文件。

这些结论说明当前处理结果具有较好的可复查性和来源追踪能力，但不应把终端录像解析结果解释为学生全部认知过程、所有按键行为或精确执行时序的完整记录。

## 11. 推荐阅读入口

- [操作系统实验任务说明](操作系统实验/README.md)
- [已清洗数据与报告说明](操作系统实验/操作系统实验数据记录-已清洗/README.md)
- [时间线验收说明](操作系统实验/历史验证记录/timeline_acceptance.md)
- [当前终端重放与报告生成入口](操作系统实验/replay_term_qa.py)
- [时间线对齐实现](操作系统实验/timeline_alignment.py)
- [时间线报告生成实现](操作系统实验/timeline_reports.py)
