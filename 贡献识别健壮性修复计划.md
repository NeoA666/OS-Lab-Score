# AI/人工贡献识别 —— 健壮性修复计划

- 文档日期：2026-09-19
- 适用范围：`操作系统实验数据清洗/AI人工贡献识别工具/`（v3，schema `ai-human-contribution-assessment/v3`）
- 文档状态：**计划（未实施）**
- 触发案例：学生 2406080118 杨涵清 lab1

---

## 1. 背景

贡献识别工具按“学生目录 + labN”读取已清洗材料，调用 NVIDIA NIM 做两轮语义工作（主分析 + 独立复核），产出 `assessment_labN.json` 和两份 Markdown。协议要求模型在受控读取后返回严格 JSON：

```json
{"action": "submit_assessment", "assessment": { ... }}
```

杨涵清 lab1 在该协议上出现**偶发、可自愈但被永久固化**的失败：同一输入指纹失败两次、成功一次。本计划给出分层、可验证、健壮性优先的修复方案。

## 2. 现状与证据

实验室记录 `AI人工贡献识别运行日志/杨涵清/lab1/runs/`：

| 时间戳 | 结果 | 关键字段 |
| --- | --- | --- |
| 20260918T174212 | `analysis_failed` | `AssessmentValidationError`，`retryable: false`，`stage: semantic_analysis_or_review` |
| 20260918T174310 | `analysis_failed` | 同上（同一输入指纹 `sha256:b5760ba9…`） |
| 20260918T175642 | `analysis_completed` | `primary_read_count: 4`，`primary_repaired: true`，`reviewer_read_count: 2` |

辅助观察：

- 抓取模型原始返回后重跑，主分析在 1–4 轮读取内完成（`MAX_READ_ROUNDS = 8`，未打满）。
- 连续 3 次受控复现全部成功（每次 2–3 个模型调用）。
- 成功那次的第一次 `submit_assessment` 被契约校验打回，靠单次修复（`primary_repaired: true`）通过。

结论：**模型偶发不满足 JSON 动作契约**是触发点；**工具把它当作不可重试的硬失败**是它变成阻塞的原因。

## 3. 根因分析

### 3.1 触发层（模型侧）

`_submission_from_response`（`analysis.py:534-536`）在 `raw.get("action") != "submit_assessment"` 时抛出：

```
AssessmentValidationError: 模型应返回 action=submit_assessment
```

模型偶尔返回缺 `action`、`action` 值错误或结构漂移的对象。这是生成式模型的固有抖动，**不可能靠一次性校验消除**，只能靠重试/降复杂度/降随机性来收敛。

### 3.2 放大层（设计侧）

1. **只有一次协议修复**：`_controlled_exchange`（`analysis.py:604-621`）在首次动作不合规时发一条 repair 消息；再不合规直接抛出。
2. **不可重试标记**：`AssessmentValidationError` 继承 `ContributionRecognitionError`，**不是** `NimError`，所以 `cli.py:145` 计算的 `retryable = False`。
3. **批次固化失败**：`_previous_failed_result`（`cli.py:223-244`）只要清单里 `analysis_status == "failed"`，就在后续 `batch` 中返回 `skipped_failed`，除非 `--retry-failed`/`--force`。
4. **契约过重**：`assessment` 是深层嵌套、多必填字段、且证据行号必须落在已读片段内的对象；模型本身就常需一次修复。

### 3.3 排除项（修正前期推断）

- **读取预算不是已证实原因**：实测最多 4/8 轮，且 3 次复现都以 2–3 轮完成。`MAX_READ_LINES_TOTAL=2400` 对超大 lab 仍是潜在风险，但不是本例主因（见 §6.5）。
- **配置错误是另一类**：`115721/120054` 的 `NimConfigurationError` 由缺少 `NVIDIA_API_KEY` 引起，与本问题无关，不应被重试掩盖。
- **数据问题不存在**：`missing_ai_reply_or_diff_hunk` 的 `insufficient_data` 是正确降级，不能重试成“有结论”。

## 4. 目标与非目标

**目标**

1. 瞬态协议失败在无人干预下自愈，且重试有界、可观测、可关闭。
2. 失败分类准确：瞬态重试、确定性保留、资料不足降级，三者不混淆。
3. 不破坏既有不变量：不伪造归因、不落密钥、原子写、可缓存、可回滚。
4. 单次抖动不再永久阻塞该 lab；连续失败仍以结构化记录暴露给教师。

**非目标**

- 不追求“任何模型输出都能被接受”；证据不足仍必须 `indeterminate`。
- 不引入第三方重试框架；用标准库实现。
- 不改动上游清洗工具与差异报告工具。

## 5. 健壮性设计原则

1. **分层隔离**：传输 / 协议 / 任务 / 批次 四层各自有界重试，互不干扰。
2. **有界与幂等**：每次重试有明确上限与预算；重复执行结果一致，不产生重复副作用。
3. **失败分类驱动**：重试只针对瞬态；确定性失败快速失败，避免烧额度。
4. **上下文重置**：整段重试用全新对话，避免被污染的历史上下文放大错误。
5. **可观测**：失败必须留下机器可读、已脱敏的诊断（分支、轮次、动作、长度摘要）。
6. **默认安全**：默认行为保守；新能力用显式配置开启，旧行为可通过开关恢复。
7. **不伪造证据**：重试耗尽后仍是结构化 `failed`，绝不降级为推测性归因。

## 6. 分层修复方案

### 6.1 L1 传输层（现状保留）

`nim_client.py` 已对 5xx/429、连接中断、SSE 截断做 2/4/8/16s 重试（`RETRY_DELAYS_SECONDS`，`MAX_RETRIES=4`）。**保持不动**，仅补充：

- 为重试加入 ±20% 抖动，避免批量并发时的重试风暴。

### 6.2 L2 协议层：动作契约容错（核心）

在 `_controlled_exchange` 内区分**读取轮**与**提交轮**的失败语义：

- 动作不合规（缺 `action` / 值错误 / JSON 无法解析 / 内容为空）→ 归类为 `TransientProtocolError`（可重试）。
- 修复提示从“请返回符合协议”升级为**附带最小 schema 骨架**，并明确“只输出一个 JSON 对象、无前后文字”。
- 预算耗尽轮（`_read_budget_exhausted_payload`）追加两条强约束：
  - 该轮**关闭 thinking**（`chat_template_kwargs.enable_thinking=false`）；
  - 显式列出 `assessment` 的必填字段名，降低结构漂移。

### 6.3 L3 任务层：整段重试（核心）

在 `cli.py:_execute_task` 对 `ContributionAnalyzer.analyze(...)` 增加有界重试：

```text
MAX_PHASE_ATTEMPTS = 3          # 主分析+复核视为一个整体单元
退避：base 2s，指数 + 抖动，上限 30s
每次重试：全新 messages（保留同一 snapshot 与 fingerprint）
可重试条件：TransientProtocolError / NimTransportError(retryable) / NimStreamError(截断)
不可重试：NimConfigurationError / 资料不足 / 校验器对“结构正确提交”的内容级拒绝
```

要点：

- 重试边界放在 **任务** 而不是单条消息，才能摆脱被污染的对话历史。
- 保留`--force`（忽略缓存）、`--retry-failed`（批次层显式重试）；新增 `--max-attempts N` 与 `--no-retry`。
- 单次运行设置**全局尝试预算**（例如 `--attempt-budget`），防止坏模型下无限消耗。

### 6.4 L4 批次层：失败保留策略修正

- `_previous_failed_result` 改为读取清单中的 `last_retryable` 与 `attempts`：
  - 标记为可重试且 `attempts < MAX_PHASE_ATTEMPTS` → 本次 `batch` **自动重试**；
  - 标记为不可重试或已达上限 → 保留为 `skipped_failed`（行为同现状）。
- 新增 `--no-retry` 让用户恢复“首次失败即固化”的旧行为。
- 批次结束汇总新增 `retried`、`retry_exhausted` 计数，总览“说明”列注明重试结果。
- **熔断**：同一批次内瞬态失败比例超阈值（如 50% 且样本 ≥ 4）时中止并明确提示，避免额度与时间失控。

### 6.5 L5 输入与预算层（次要，防御性）

- 材料总量超过读取预算时，**优先裁剪 diff 报告**（保留 hunk 头与关键上下文），或提高 `MAX_READ_LINES_TOTAL` 到能覆盖 P95 lab（建议 ≥3200）。
- 预算耗尽后仍拿不到提交时，允许“只读已获取片段”的降级提交提示（现有机制），但**必须**保持证据边界。

### 6.6 L6 可观测层

`analysis_failed` 运行日志补充**已脱敏**诊断字段：

```json
{
  "event": "analysis_failed",
  "stage": "semantic_analysis_or_review",
  "phase": "primary",
  "attempt": 2,
  "round_index": 5,
  "budget_exhausted": true,
  "last_action": "read_material",
  "content_length": 155,
  "content_sha256": "…",
  "error_code": "TransientProtocolError",
  "retryable": true
}
```

约束：只记录动作名、轮次、长度、哈希等不可逆元数据；**绝不落原始模型内容、思维内容或密钥**（沿用 `redact_sensitive_text` 与 `_strip_disallowed_output_fields`）。

## 7. 失败分类矩阵

| 情形 | 代码来源 | 分类 | 动作 |
| --- | --- | --- | --- |
| 缺 `action` / 动作值错误 / JSON 不可解析 / 空内容 | `analysis.py:534-536,141-180` | 瞬态 | L2 修复 → L3 重试 |
| SSE 截断 / 连接中断 / 5xx / 429 | `nim_client.py` | 瞬态 | L1 重试 → L3 兜底 |
| `read_material` 请求非法 | `analysis.py:435-458` | 瞬态 | L2 修复（现有）→ L3 |
| `analysis` 契约校验对“结构正确提交”的内容级拒绝 | `analysis.py` validator | 半确定性 | 有限重试（1 次） |
| 资料不足（`missing_ai_reply_or_diff_hunk`） | `analysis.py:1347` | 确定性 | `insufficient_data`，不重试 |
| 缺 `NVIDIA_API_KEY` / base_url 非法 | `protocol.py:90-115` | 确定性 | 配置失败，不重试 |
| 材料缺失/不安全/损坏 | `repository.py` | 确定性 | 结构化失败 |

## 8. 数据结构与清单变更

- `storage.py` 清单 `labs[lab]` 增加：`attempts`、`last_retryable`、`last_error_code`、`first_failed_at`、`next_retry_after`（可选）。
- 相应提升 `MANIFEST_SCHEMA_VERSION`；旧清单缺失字段时按“不可重试历史失败”处理，保证向后兼容。
- `cache_hit` 语义不变（仅 `complete`/`insufficient_data` 命中）。
- 所有写入继续走 `atomic_write_json`/`atomic_write_bundle`，保证重试期间不出现半写状态。

## 9. 配置项（默认保守）

| 配置 | 默认 | 说明 |
| --- | --- | --- |
| `--max-attempts` | 3 | 每任务整段重试上限 |
| `--no-retry` | 关 | 恢复首次失败即固化 |
| `--attempt-budget` | 有默认上限 | 单次运行整段重试总预算 |
| `NVIDIA_PROTOCOL_TEMPERATURE` | 0.2 | 仅提交/修复轮的采样温度 |
| `NVIDIA_FALLBACK_MODEL` | 空 | 可选的协议修复备用模型 |
| `NVIDIA_READ_ROUNDS` | 8 | 可调读轮上限（防御性） |

## 10. 代码改动清单

| 文件 | 改动 |
| --- | --- |
| `contribution_recognition/analysis.py` | 新增 `TransientProtocolError`；拆分读取轮/提交轮失败分类；增强 repair/预算耗尽提示；提交轮可关 thinking；引入重试边界所需的纯函数 |
| `contribution_recognition/cli.py` | `_execute_task` 增加有界重试与退避；`_persist_failure` 修正 `retryable` 判定；`_previous_failed_result` 读取重试元数据；`_batch` 熔断与计数 |
| `contribution_recognition/protocol.py` | `NimAnalysisSettings/NimConfig` 增加协议温度、备用模型、读轮上限等字段与校验 |
| `contribution_recognition/storage.py` | 清单字段与版本升级；诊断性元数据写入 |
| `contribution_recognition/nim_client.py` | 重试加抖动；支持 per-call 覆盖 `enable_thinking`/temperature |
| `identify_contributions.py` | 新增 CLI 参数 |
| `README.md` / `诚信判别系统对接文档.md` | 记录新参数、重试语义与失败分类 |
| `test_nim_client.py` 等 | 见 §11 |

## 11. 测试计划

**单元**

1. 脚本化假客户端：动作错误 1 次后成功 → 任务成功且记录 1 次重试。
2. 连续动作错误达到上限 → 结构化 `failed`，`retryable=true`，不无限循环。
3. 预算耗尽路径：强制提交成功 / 仍失败触发整段重试。
4. 分类矩阵逐条：配置错误、资料不足不重试；传输错误重试。
5. 清单重试元数据与版本兼容（旧清单不误判为可重试）。
6. 运行日志诊断字段齐全且**不含密钥/原始内容**（对 `nvapi-` 与 `reasoning` 做断言）。

**集成**

7. 端到端夹具：注入“第一次提交动作不合规”的假客户端 → 全流程成功，产物与缓存一致。
8. 批次层：可重试失败自动重试；`--no-retry` 恢复旧行为；熔断触发。
9. 幂等性：同一任务重复运行结果一致，不产生重复报告/清单漂移。

**回归**

10. 现有 27 项测试全部通过。**前置修复**：`test_v3_reporting.py:133-134` 同时 `assertIn/assertNotIn("[REDACTED]")` 的自相矛盾；`test_repository.py` 的 `/ ` 与 `\` 路径断言（Windows）。

**性质/边界**

11. 尝试次数、退避、预算上界可证明有界。
12. 重试期间中断不产生半写产物（原子写 + 中断注入）。

## 12. 验收标准

- 复现用例（杨涵清 lab1 同指纹）在**无人工干预**下于 ≤3 次尝试内完成。
- 首次瞬态失败不再直接变成 `skipped_failed`；连续失败在日志与总览中可见。
- 资料不足、配置错误行为与现状一致。
- 清洗产物无密钥、无原始模型内容；所有清单/报告哈希自洽。
- 现有回归全绿（含前置修复的两项）。
- 单任务额外 NIM 调用有明确上限；批次有熔断。

## 13. 灰度与回滚

- 阶段一：失败分类 + L3 整段重试（低风险，先合）。
- 阶段二：清单元数据 + L4 批次自动重试 + CLI 开关。
- 阶段三：提示加固（schema 骨架、提交轮关 thinking）+ 抖动/熔断。
- 每阶段独立可回滚；`--no-retry` 是全局保守回退开关。
- 回滚不要求删除已有产物：`failed` 记录在新旧逻辑下都可被 `--retry-failed` 重新处理。

## 14. 风险与权衡

| 风险 | 缓解 |
| --- | --- |
| 重试放大额度消耗 | 有界尝试 + 全局预算 + 熔断 + 抖动 |
| 重试掩盖真实确定性缺陷 | 严格分类矩阵；确定性错误不重试 |
| 降低温度影响语义质量 | 仅提交/修复轮降温度，分析与复核主轮不变 |
| 清单新增字段破坏兼容 | 版本升级 + 缺字段按不可重试处理 |
| 诊断日志泄露敏感信息 | 只落动作/长度/哈希；沿用脱敏与字段白名单 |

## 15. 待确认问题

1. 整段重试默认次数取 3 是否合适（额度 vs 成功率）。
2. 是否默认开启批次自动重试，还是需要显式 `--retry-failed`（当前默认保守）。
3. 是否需要备用模型（`NVIDIA_FALLBACK_MODEL`）专用于协议修复。
4. 提交轮关闭 thinking 是否影响复核一致性（需小样本 A/B）。
5. 两处既有失败测试是随本次一并修复，还是单独提交。

## 附录 A：关键代码位置

- 受控对话与失败点：`contribution_recognition/analysis.py:534-671`
- 读取预算常量：`analysis.py:38-46`
- 预算耗尽提示：`analysis.py:543-549`
- 任务执行与失败持久化：`contribution_recognition/cli.py:137-180,283-435`
- 失败保留：`cli.py:223-244`
- 缓存语义：`contribution_recognition/storage.py:523-555`
- 传输重试：`contribution_recognition/nim_client.py:33-123`
- 错误层级：`contribution_recognition/errors.py`、`analysis.py:87-91`、`protocol.py:29-70`

## 附录 B：现状不变量（必须保持）

- 失败绝不转化为推测性归因；资料不足固定 `indeterminate`。
- 密钥只从环境/本地忽略文件读取，绝不写入任何产物。
- 报告只含脱敏短摘录，不含完整输入或模型私有输出。
- 原始录像、原始提交、基准目录只读。
- 所有清单与报告原子写入，拒绝符号链接与越界路径。
