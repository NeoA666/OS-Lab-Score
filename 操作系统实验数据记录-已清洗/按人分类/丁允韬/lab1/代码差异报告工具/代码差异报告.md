# lab1 源码差异报告

- 学号：2406080201
- 姓名：丁允韬
- 数据采集时间：2026-09-16 11:59
- 原始提交目录：操作系统实验数据记录/2406080201-丁允韬-20260916-1159
- 基准目录：xv6-ai-labs-km-无答案/lab1
- 学生源码指纹：8860a4e10e00d9990a9a6883142f6ab7f620816971c64fd5009f35990a19c375
- 基准源码指纹：bb45ef70570a36af1ee1ca329d9bc7c64c92d8949623007e59a389c3c546a4c2
- 分析器签名：ad5a9cea6da0c2ab26da01ac7f984ecd7d2791bc12e670019894b4a4344f9045
- 比较范围：kernel、xv6-user、linker 下的源码/脚本/构建配置，以及根目录 Makefile
- 空白符策略：忽略空格、Tab、空白行和行尾空白差异

## 统计

| 项目 | 数量 |
| --- | ---: |
| 纳入比较的文件 | 107 |
| 一致文件 | 103 |
| 有差异文件 | 4 |
| 新增行 | 7 |
| 删除行 | 3 |
| 处理异常文件 | 0 |

## 差异详情

### `kernel/include/sysnum.h`（修改）

```diff
diff --git 基准/kernel/include/sysnum.h 学生/kernel/include/sysnum.h
index fe4492e..35d9c48 100644
--- 基准/kernel/include/sysnum.h
+++ 学生/kernel/include/sysnum.h
@@ -28,6 +28,7 @@
 #define SYS_readdir     24
 #define SYS_getcwd      25
 #define SYS_rename      26
+#define SYS_halt        27
 
 // ═══════════════════════════════════════════════════════════════
 // 【AI-CHECKPOINT ①】SYS_halt 编号（主线·教学）
@@ -56,6 +57,6 @@
 //   - 你选的号和已有 syscall 冲突了吗？
 //   - syscalls[] 用这个号做下标会越界吗？
 // ───────────────────────────────────────────────────────────
-#define SYS_halt   0   // ← 占位。学生：思考后改成未占用号
+// ← 占位。学生：思考后改成未占用号
 
 #endif
```

### `kernel/syscall.c`（修改）

```diff
diff --git 基准/kernel/syscall.c 学生/kernel/syscall.c
index 0219b80..d679740 100644
--- 基准/kernel/syscall.c
+++ 学生/kernel/syscall.c
@@ -145,6 +145,7 @@ static uint64 (*syscalls[])(void) = {
   [SYS_trace]       sys_trace,
   [SYS_sysinfo]     sys_sysinfo,
   [SYS_rename]      sys_rename,
+  [SYS_halt]        sys_halt,
   // ═══════════════════════════════════════════════════════════════
   // 【AI-CHECKPOINT ③】syscall.c syscalls[] dispatch（主线·教学）
   // ────────────────────────────────────────────────────────────────
@@ -199,6 +200,7 @@ static char *sysnames[] = {
   [SYS_trace]       "trace",
   [SYS_sysinfo]     "sysinfo",
   [SYS_rename]      "rename",
+  [SYS_halt]        "halt",
 };
 
 void
```

### `kernel/sysproc.c`（修改）

```diff
diff --git 基准/kernel/sysproc.c 学生/kernel/sysproc.c
index 809b911..a19a5e6 100644
--- 基准/kernel/sysproc.c
+++ 学生/kernel/sysproc.c
@@ -185,6 +185,6 @@ sys_trace(void)
 uint64
 sys_halt(void)
 {
-  // todo（CP④）：通过 SBI 提供的关机封装结束系统
-  return -1;  // 占位——学生实现后替换为实际关机逻辑
+  sbi_shutdown();// todo（CP④）：通过 SBI 提供的关机封装结束系统
+  return 0;  // 占位——学生实现后替换为实际关机逻辑
 }
```

### `xv6-user/usys.pl`（修改）

```diff
diff --git 基准/xv6-user/usys.pl 学生/xv6-user/usys.pl
index 2e7f4bc..2ce6d2a 100644
--- 基准/xv6-user/usys.pl
+++ 学生/xv6-user/usys.pl
@@ -41,6 +41,7 @@ entry("remove");
 entry("trace");
 entry("sysinfo");
 entry("rename");
+entry("halt");
 
 # ═══════════════════════════════════════════════════════════════
 # 【AI-CHECKPOINT ②】usys.pl entry("halt")（主线·教学）
```
