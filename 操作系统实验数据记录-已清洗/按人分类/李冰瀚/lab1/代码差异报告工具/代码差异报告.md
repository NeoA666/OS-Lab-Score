# lab1 源码差异报告

- 学号：2406080213
- 姓名：李冰瀚
- 数据采集时间：2026-09-16 18:05
- 原始提交目录：操作系统实验数据记录/2406080213-李冰瀚-20260916-1805
- 基准目录：xv6-ai-labs-km-无答案/lab1
- 学生源码指纹：6984fbbf8314a9e699f0c18ca78a3496d7c34bab4004d230f845d79a974c3fc8
- 基准源码指纹：bb45ef70570a36af1ee1ca329d9bc7c64c92d8949623007e59a389c3c546a4c2
- 分析器签名：ad5a9cea6da0c2ab26da01ac7f984ecd7d2791bc12e670019894b4a4344f9045
- 比较范围：kernel、xv6-user、linker 下的源码/脚本/构建配置，以及根目录 Makefile
- 空白符策略：忽略空格、Tab、空白行和行尾空白差异

## 统计

| 项目 | 数量 |
| --- | ---: |
| 纳入比较的文件 | 107 |
| 一致文件 | 102 |
| 有差异文件 | 5 |
| 新增行 | 11 |
| 删除行 | 5 |
| 处理异常文件 | 0 |

## 差异详情

### `Makefile`（修改）

```diff
diff --git 基准/Makefile 学生/Makefile
index f727dde..b5683eb 100644
--- 基准/Makefile
+++ 学生/Makefile
@@ -151,6 +151,12 @@ ifeq ($(platform), k210)
 else
 	@$(QEMU) $(QEMUOPTS)
 endif
+# 调试⽤：启动带 gdbstub 的 QEMU（端⼝ 1234），停在起点等 gdb 接管
+# 另开⼀个终端跑：gdb-multiarch target/kernel -ex 'set arch riscv:rv64' -ex 'target remote localhost:1234'
+gdb: build
+	@echo "*** QEMU 已在 :1234 等待 gdb，请在另⼀个终端运⾏: ***"
+	@echo " gdb-multiarch target/kernel -ex 'set arch riscv:rv64' -ex 'target remote localhost:1234'"
+	@$(QEMU) $(QEMUOPTS) -S -s
 
 $U/initcode: $U/initcode.S
 	$(CC) $(CFLAGS) -march=rv64g -nostdinc -I. -Ikernel -c $U/initcode.S -o $U/initcode.o
```

### `kernel/include/sysnum.h`（修改）

```diff
diff --git 基准/kernel/include/sysnum.h 学生/kernel/include/sysnum.h
index fe4492e..ac00d61 100644
--- 基准/kernel/include/sysnum.h
+++ 学生/kernel/include/sysnum.h
@@ -56,6 +56,6 @@
 //   - 你选的号和已有 syscall 冲突了吗？
 //   - syscalls[] 用这个号做下标会越界吗？
 // ───────────────────────────────────────────────────────────
-#define SYS_halt   0   // ← 占位。学生：思考后改成未占用号
+#define SYS_halt   27   // ← 占位。学生：思考后改成未占用号
 
 #endif
\ No newline at end of file
```

### `kernel/syscall.c`（修改）

```diff
diff --git 基准/kernel/syscall.c 学生/kernel/syscall.c
index 0219b80..6e04da1 100644
--- 基准/kernel/syscall.c
+++ 学生/kernel/syscall.c
@@ -169,7 +169,7 @@ static uint64 (*syscalls[])(void) = {
   //   - 号、声明、数组项、处理函数四处名字对应吗？
   //   - 未注册时 syscall() 会走到哪条分支？
   // ───────────────────────────────────────────────────────────
-  // [SYS_halt]        sys_halt,  // TODO: 学生自行添加此行（CP③）
+ [SYS_halt]        sys_halt,
 };
 
 static char *sysnames[] = {
```

### `kernel/sysproc.c`（修改）

```diff
diff --git 基准/kernel/sysproc.c 学生/kernel/sysproc.c
index 809b911..eb3132d 100644
--- 基准/kernel/sysproc.c
+++ 学生/kernel/sysproc.c
@@ -185,6 +185,6 @@ sys_trace(void)
 uint64
 sys_halt(void)
 {
-  // todo（CP④）：通过 SBI 提供的关机封装结束系统
-  return -1;  // 占位——学生实现后替换为实际关机逻辑
+  sbi_shutdown();
+  return 0;
 }
```

### `xv6-user/usys.pl`（修改）

```diff
diff --git 基准/xv6-user/usys.pl 学生/xv6-user/usys.pl
index 2e7f4bc..fc556d8 100644
--- 基准/xv6-user/usys.pl
+++ 学生/xv6-user/usys.pl
@@ -64,4 +64,4 @@ entry("rename");
 #   - 用户态桩函数是否使用了和 sysnum.h 一致的 syscall 名称？
 #   - ecall 后的路径能和 syscall.c 的分发表连起来吗？
 # ───────────────────────────────────────────────────────────
-# entry("halt");  // TODO: 学生自行添加此行（CP②）
+entry("halt");
```
